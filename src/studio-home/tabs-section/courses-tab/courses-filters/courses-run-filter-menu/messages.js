import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  coursesRunFilterMenuAll: {
    id: 'course-authoring.studio-home.courses.tab.org-filter-menu.all-run',
    defaultMessage: 'All courses run',
  },
});

export default messages;